{% extends 'base.html.twig' %}

{% block title %}Admin - New {% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
        <h2 class="display-5 my-5">Ajouter une oeuvre</h2>
            <div class="col-md-8 offset-2">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-outline-secondary">Ajoutez</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}